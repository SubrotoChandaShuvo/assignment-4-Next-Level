model Category {
  id          String      @id @default(cuid())
  name        String      @unique
  description String?

  gearItems   GearItem[]

  createdAt   DateTime    @default(now())
  updatedAt   DateTime    @updatedAt
}